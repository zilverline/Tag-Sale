<h1>Welcome <%= @user.name %></h1>

<h3>Current Item Listed for Sale</h3>
<ul>
    <% @user.items.each do |item| %>
    <li><%= item.name %></li>
    <% end %>
</ul>

<%= link_to "Create a Listing", new_item_path %>
